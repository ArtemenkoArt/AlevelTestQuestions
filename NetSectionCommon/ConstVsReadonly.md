# Const vs Readonly

## ѕолезные ссылки

[Const and Readonly(metanit)](https://metanit.com/sharp/tutorial/3.3.php)

## Const

 онстанты предназначены дл€ описани€ таких значений, которые не должны измен€тьс€ в программе. ƒл€ определени€ констант используетс€ ключевое слово const.

Const:

* ** онстанты не могут быть определены как static, они по умолчанию static**

* ** онстанта должна быть проинициализирована при определении, так как во врем€ компил€ции const должна быть уже определена**


* **ѕосле определени€ значение константы не может быть изменено**

```csharp
class MathLib
{
    public const double PI=3.141;
}
 
class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine(MathLib.PI);
    }
}
```
## Readonly

ѕол€ readonly можно инициализировать при их объ€влении либо на уровне класса, либо инициилизировать и измен€ть в конструкторе. »нициализировать или измен€ть их значение в других местах нельз€, можно только считывать их значение. лючевое слово readonly.

Readonly:

* **ћогут быть определены как static**

* **«начение пол€ присваиваетс€ во врем€ выполнени€ программы(при создании объекта)**

* **ћогут быть проиницилизированы либо при объ€влении, либо в конструкторе**

```csharp
public class A
{
    public readonly double number1 = 12.21;

    public A()
    {
        number1 = 42.42;
    }
}

public class Program
{
    static void Main(string[] args)
    {

        var tempA = new A();

        Console.WriteLine(tempA.number1);
    }
}
```
## ¬ывод
* **Const должны быть определены во врем€ компил€ции, а readonly могут быть определены во врем€ выполнени€ программы.**

* **»нициализировать const можно только при ее определении. Readonly можно инициализировать либо при его определении, либо в конструкторе класса.**

* **Const не могут быть статическими. Readonly могут быть статическими.**